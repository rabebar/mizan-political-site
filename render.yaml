services:
  - type: web
    name: mizan-political-site
    runtime: static
    buildCommand: "true"
    staticPublishPath: .
    autoDeployTrigger: commit
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
